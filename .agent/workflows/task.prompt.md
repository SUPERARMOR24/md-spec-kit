# ゴール
`output/plan.md` から実行可能なタスクリストを `output/task.md` に日本語で作成する。

# 手順
1. プランの各ステップを 10〜20 分で終わる粒度に細分化する（高校生の実習を想定）。
2. 依存関係が分かる順序で並べる。並列作業は避ける。
3. 各タスクに「目的」「完了条件」「チェックボックス」を付ける。

# 出力フォーマット
- ファイル: `output/task.md`
- `templates/template.task.md` の枠に沿って箇条書きで記述。
